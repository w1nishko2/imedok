# Документация: Парсинг рецептов с нескольких источников

## Проблема

Сайт **1000.menu** использует:
1. **Классическую пагинацию** на некоторых страницах (`?page=N`)
2. **Infinite Scroll** на новых разделах (динамическая подгрузка через JS)

Старый парсер работал только с пагинацией и мог пропускать рецепты.

## Решение

Реализован **умный парсер**, который:
- ✅ Работает с **несколькими источниками** одновременно
- ✅ Поддерживает **пагинацию** (`?page=1`, `?page=2`, и т.д.)
- ✅ **НЕ требует headless браузер** (быстро и легко!)
- ✅ Собирает URL всех рецептов со всех страниц
- ✅ **Автоматически фильтрует** уже существующие в БД
- ✅ Останавливается при достижении цели

## Установка

Не требуется! Парсер использует только Guzzle HTTP (уже установлен в Laravel).

## Использование

### 1. Базовая команда (собрать 50 новых рецептов)

```bash
php artisan recipes:parse-infinite-scroll
```

**Что происходит:**
- Парсит 3 источника: `cooking/all-new`, `cooking`, `catalog`
- Собирает URL рецептов с каждого источника
- Фильтрует только новые (которых нет в БД)
- Останавливается при достижении 50 новых рецептов

### 2. Указать количество рецептов

```bash
php artisan recipes:parse-infinite-scroll --count=100
```

### 3. Добавить свой URL для парсинга

```bash
php artisan recipes:parse-infinite-scroll --url="https://1000.menu/cooking/popular"
```

Это **добавит** URL к стандартным источникам (итого будет 4 источника).

### 5. Сразу спарсить найденные рецепты

```bash
php artisan recipes:parse-infinite-scroll --count=30 --parse-now
```

⚠️ **Внимание:** Флаг `--parse-now` запустит полный парсинг всех найденных рецептов. Это займет МНОГО времени (2-3 секунды на рецепт).

## Как это работает

### Алгоритм:

1. **Запуск headless браузера** (Chrome в невидимом режиме)
2. **Открытие целевой страницы** (например, https://1000.menu/cooking/all-new)
3. **Ожидание загрузки** первых карточек рецептов
4. **Цикл скроллинга:**
   - Скроллим страницу вниз (`window.scrollTo(0, document.body.scrollHeight)`)
   - Ждем 2 секунды для подгрузки новых карточек
   - Извлекаем все ссылки на рецепты (паттерн: `/cooking/ЧИСЛО`)
   - Фильтруем дубликаты
   - Проверяем в БД (оставляем только новые)
   - Повторяем, пока не достигнем цели или конца контента
5. **Возврат списка новых URL**

### Критерии остановки:

- ✅ Собрано нужное количество новых рецептов
- ✅ Достигнут конец контента (3 скролла подряд без новых карточек)
- ✅ Превышен лимит скроллов (защита от зависания)

## Примеры использования

### Пример 1: Быстрый сбор 20 новых рецептов

```bash
php artisan recipes:parse-infinite-scroll --count=20
```

**Результат:**
```
╔════════════════════════════════════════════════════════╗
║   🚀 Парсинг рецептов с Infinite Scroll               ║
╚════════════════════════════════════════════════════════╝

🔗 URL: https://1000.menu/cooking/all-new
🎯 Цель: 20 новых рецептов
📜 Макс. скроллов: 50
⏱️  Пауза между скроллами: 2 сек

🌐 Запуск headless браузера...
⚠️  Это может занять несколько минут...

📜 Начинаем скроллинг...
🔄 Скролл #1: найдено 24 карточек (+24 новых)
✅ Всего собрано URL: 24, новых (не в БД): 20/20

🎉 Цель достигнута! Собрано 20 новых рецептов

╔════════════════════════════════════════════════════════╗
║   ✅ Сбор URL завершен успешно!                       ║
╚════════════════════════════════════════════════════════╝

📊 Найдено новых рецептов: 20
```

### Пример 2: Полный цикл (сбор + парсинг)

```bash
php artisan recipes:parse-infinite-scroll --count=10 --parse-now
```

**Результат:**
```
🔍 Начинаем парсинг найденных рецептов...
⚠️  Это займет много времени!

 10/10 [▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓] 100%

╔════════════════════════════════════════════════════════╗
║   🎉 Парсинг завершен!                                ║
╚════════════════════════════════════════════════════════╝

✅ Успешно спарсено: 10
❌ Ошибок: 0
```

## Настройка для Crontab

### Вариант 1: Ежедневный сбор новых рецептов (ночью)

```cron
# Каждый день в 3:00 - собрать 50 новых рецептов
0 3 * * * cd /home/g/gamechann2/im-edok_ru && php8.1 artisan recipes:parse-infinite-scroll --count=50 >> storage/logs/infinite-scroll-cron.log 2>&1
```

### Вариант 2: Сбор + парсинг (выходные дни)

```cron
# Каждую субботу в 2:00 - собрать и спарсить 30 рецептов
0 2 * * 6 cd /home/g/gamechann2/im-edok_ru && php8.1 artisan recipes:parse-infinite-scroll --count=30 --parse-now >> storage/logs/infinite-scroll-cron.log 2>&1
```

### Вариант 3: Несколько запусков в день

```cron
# Каждые 6 часов - собрать 20 новых рецептов
0 */6 * * * cd /home/g/gamechann2/im-edok_ru && php8.1 artisan recipes:parse-infinite-scroll --count=20 >> storage/logs/infinite-scroll-cron.log 2>&1
```

## Логирование

Все действия парсера логируются в:
- **Laravel Log:** `storage/logs/laravel.log`
- **Cron Log:** `storage/logs/infinite-scroll-cron.log` (если настроен crontab)

**Примеры логов:**

```log
[2025-11-04 18:00:00] production.INFO: 🎯 Запуск парсинга infinite scroll: цель 50 НОВЫХ рецептов
[2025-11-04 18:00:00] production.INFO: 🔗 URL для парсинга: https://1000.menu/cooking/all-new
[2025-11-04 18:00:05] production.INFO: 🌐 Открытие страницы...
[2025-11-04 18:00:08] production.INFO: 📜 Начинаем скроллинг...
[2025-11-04 18:00:10] production.INFO: 🔄 Скролл #1: найдено 24 карточек (+24 новых)
[2025-11-04 18:00:10] production.INFO: ✅ Всего собрано URL: 24, новых (не в БД): 20/50
[2025-11-04 18:00:13] production.INFO: 🔄 Скролл #2: найдено 48 карточек (+24 новых)
[2025-11-04 18:00:13] production.INFO: ✅ Всего собрано URL: 48, новых (не в БД): 42/50
[2025-11-04 18:00:16] production.INFO: 🔄 Скролл #3: найдено 72 карточек (+24 новых)
[2025-11-04 18:00:16] production.INFO: ✅ Всего собрано URL: 72, новых (не в БД): 51/50
[2025-11-04 18:00:16] production.INFO: 🎉 Цель достигнута! Собрано 51 новых рецептов
[2025-11-04 18:00:16] production.INFO: 🏁 Парсинг завершен:
[2025-11-04 18:00:16] production.INFO:    📊 Всего URL собрано: 72
[2025-11-04 18:00:16] production.INFO:    ✨ Новых рецептов (не в БД): 51
[2025-11-04 18:00:16] production.INFO:    📜 Выполнено скроллов: 3
[2025-11-04 18:00:16] production.INFO: 🔒 Браузер закрыт
```

## Производительность

### Скорость работы:

- **Скролл:** ~2-3 секунды (+ пауза 2 сек)
- **Сбор 50 URL:** ~2-5 минут
- **Парсинг 1 рецепта:** ~2-3 секунды
- **Полный цикл 50 рецептов:** ~10-15 минут

### Оптимизация:

1. **Уменьшить паузу:** `--scroll-pause=1` (риск пропуска карточек)
2. **Увеличить лимит скроллов:** `--max-scrolls=100` (для большого сбора)
3. **Использовать отдельные задачи:**
   - Сначала собрать URL: `recipes:parse-infinite-scroll --count=100`
   - Потом парсить фоном через очереди

## Требования

### Системные:

- ✅ PHP 8.0+
- ✅ Composer
- ✅ Chrome/Chromium (устанавливается автоматически через Panther)
- ✅ Расширение PHP: `zip`, `curl`

### На хостинге:

Если на хостинге **нет возможности** запустить headless браузер:
1. Запускайте команду на локальном компьютере
2. Используйте VPS/VDS с root доступом
3. Или используйте облачные сервисы парсинга

## Troubleshooting

### Ошибка: "Chrome binary not found"

**Решение:**
```bash
# Linux
sudo apt-get install chromium-browser chromium-chromedriver

# macOS
brew install --cask chromium
```

### Ошибка: "Timeout waiting for"

**Решение:** Увеличьте паузу между скроллами:
```bash
php artisan recipes:parse-infinite-scroll --scroll-pause=5
```

### Не находит новые рецепты

**Решение:** Проверьте, что URL корректный и доступен:
```bash
php artisan recipes:parse-infinite-scroll --url="https://1000.menu/cooking/all-new"
```

## Сравнение с обычным парсером

| Параметр | Обычный парсер | Infinite Scroll парсер |
|----------|----------------|------------------------|
| **Тип страниц** | Статические (HTML) | Динамические (JS) |
| **Пагинация** | `?page=N` | Infinite scroll |
| **Скорость** | ⚡ Быстро | 🐢 Медленно |
| **Точность** | ⚠️ Может пропускать | ✅ Высокая |
| **Ресурсы** | 💚 Низкие | 💛 Средние |
| **Зависимости** | Guzzle | Panther + Chrome |

## Рекомендации

### Для локальной разработки:

```bash
php artisan recipes:parse-infinite-scroll --count=10 --parse-now
```

### Для продакшена (crontab):

```bash
# Только сбор URL (быстро)
php artisan recipes:parse-infinite-scroll --count=50

# Парсинг через очереди (фоном)
php artisan queue:work
```

## Полезные ссылки

- [Symfony Panther](https://github.com/symfony/panther)
- [ChromeDriver](https://chromedriver.chromium.org/)
- [Infinite Scroll на 1000.menu](https://1000.menu/cooking/all-new)
