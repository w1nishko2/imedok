# 🚀 БЫСТРЫЙ СТАРТ - Парсер 2000 рецептов/день

## ⚡ Команды для копирования в SSH (по порядку)

### 1️⃣ Подключение к хостингу
```bash
ssh gamechann2@vh303.sweb.ru
cd im-edok_ru
```

### 2️⃣ Быстрая настройка (автоматически)
```bash
chmod +x QUICK_SETUP.sh
bash QUICK_SETUP.sh
```

### 3️⃣ Добавление в CRONTAB
```bash
crontab -e
```
**Добавьте эту строку (скопируйте целиком):**
```
*/30 * * * * cd /home/g/gamechann2/im-edok_ru && php8.1 artisan recipes:parse --count=42 >> storage/logs/parser.log 2>&1
```
**Сохраните:** `Ctrl+X` → `Y` → `Enter`

### 4️⃣ Проверка что cron добавлен
```bash
crontab -l
```

### 5️⃣ Мониторинг (через 30 мин)
```bash
# Статистика
php8.1 artisan parser:status

# Логи парсера
tail -n 50 storage/logs/parser.log

# Последние рецепты
php8.1 artisan db:check
```

---

## 📊 Параметры парсинга

- **Частота:** Каждые 30 минут
- **За раз:** 42 рецепта
- **Итого:** 48 × 42 = **2016 рецептов/день** ✅
- **PHP:** 8.1

---

## 🔍 Быстрая диагностика (если что-то не так)

```bash
# Проверка прав на schedule.sh
ls -la schedule.sh

# Должно быть: -rwxr-xr-x (с x в конце)

# Если нет - выполните:
chmod +x schedule.sh

# Проверка вручную
bash schedule.sh

# Проверка результата
php8.1 artisan parser:status
```

---

## 🎯 Ожидаемый результат за 24 часа

| Показатель | Значение |
|------------|----------|
| Запусков cron | 48 (каждые 30 мин) |
| Запусков парсера | 48 (каждые 30 мин) |
| Рецептов за раз | 42 |
| **ВСЕГО рецептов** | **2016** |

---

## ⚠️ Важно!

1. ✅ Используется **php8.1** (не php)
2. ✅ Cron запускается **каждые 30 минут** (`*/30` в crontab)
3. ✅ Парсинг идет напрямую: `php8.1 artisan recipes:parse --count=42`
4. ✅ Логи в: `storage/logs/parser.log`

---

## 📞 Проблемы?

### Если ничего не работает:
```bash
echo "=== ДИАГНОСТИКА ==="
php8.1 -v
ls -la schedule.sh
crontab -l
tail -n 20 storage/logs/laravel.log
php8.1 artisan parser:status
```

Скопируйте вывод всех команд для анализа.
