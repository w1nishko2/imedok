# 🤖 Парсер рецептов - Подробная документация

## 📋 Как работает парсер

### Принцип работы

Парсер работает в 2 этапа:

1. **Сбор URL** - обходит страницы сайта и собирает ссылки на рецепты
2. **Парсинг рецептов** - загружает каждый рецепт и сохраняет в базу данных

### Ключевые особенности

✅ **Точное количество** - парсер гарантированно найдет РОВНО столько новых рецептов, сколько вы укажете  
✅ **Автоматическая фильтрация** - рецепты, которые уже есть в БД, автоматически пропускаются  
✅ **Защита от дубликатов** - проверка по `source_url` перед добавлением в базу  
✅ **Умная пагинация** - автоматически перебирает страницы до нахождения нужного количества  
✅ **Безопасные задержки** - случайные паузы между запросами (1-3 сек)

## 🚀 Запуск парсера

### Базовая команда

```bash
php artisan recipes:parse --count=30
```

**Что произойдет:**
1. Парсер начнет обходить страницы с новыми рецептами
2. С каждой страницы получит ~50-60 URL рецептов
3. Проверит в БД - какие из них уже есть
4. Продолжит обход страниц пока не найдет ровно 30 НОВЫХ рецептов
5. Спарсит каждый из 30 рецептов и добавит в БД

### Примеры использования

```bash
# Быстрый тест (5 рецептов)
php artisan recipes:parse --count=5

# Стандартная порция для cron (32 рецепта)
php artisan recipes:parse --count=32

# Большая загрузка (100 рецептов)
php artisan recipes:parse --count=100

# Массовая загрузка (500 рецептов - займет ~30-40 минут)
php artisan recipes:parse --count=500
```

## 📊 Мониторинг парсера

### Статистика

```bash
# Быстрая статистика
php artisan parser:stats

# Детальная статистика с графиками за 7 дней
php artisan parser:stats --detailed
```

**Вывод покажет:**
- Сколько всего рецептов в базе
- Сколько добавлено сегодня/вчера/за неделю/за месяц
- Среднюю скорость парсинга
- Прогноз достижения цели 1500/день
- Детальная разбивка по дням (с флагом --detailed)

### Просмотр логов

```bash
# Последние 100 строк
tail -n 100 storage/logs/parser.log

# Мониторинг в реальном времени
tail -f storage/logs/parser.log

# Только успешные добавления
grep "✅" storage/logs/parser.log

# Только ошибки
grep "❌\|ERROR" storage/logs/parser.log
```

## ⚙️ Настройка автоматического парсинга (CRON)

### Текущая настройка (1536 рецептов/день)

В файле `app/Console/Kernel.php`:

```php
$schedule->command('recipes:parse --count=32')
    ->everyThirtyMinutes()
    ->appendOutputTo(storage_path('logs/parser.log'));
```

**Расчет:**
- 48 запусков в день (каждые 30 минут)
- 32 рецепта за раз
- **Итого: 48 × 32 = 1536 рецептов/день** ✅

### Варианты конфигурации

#### Минимум 1500/день (рекомендуется)
```php
// Вариант 1: Каждые 30 минут по 32
$schedule->command('recipes:parse --count=32')
    ->everyThirtyMinutes(); // 48 × 32 = 1536/день

// Вариант 2: Каждый час по 65
$schedule->command('recipes:parse --count=65')
    ->hourly(); // 24 × 65 = 1560/день

// Вариант 3: Каждые 15 минут по 16
$schedule->command('recipes:parse --count=16')
    ->everyFifteenMinutes(); // 96 × 16 = 1536/день
```

#### Агрессивный рост (2000-3000/день)
```php
// Вариант 1: Каждые 30 минут по 50
$schedule->command('recipes:parse --count=50')
    ->everyThirtyMinutes(); // 48 × 50 = 2400/день

// Вариант 2: Каждые 20 минут по 45
$schedule->command('recipes:parse --count=45')
    ->everyTwentyMinutes(); // 72 × 45 = 3240/день

// Вариант 3: Каждые 15 минут по 32
$schedule->command('recipes:parse --count=32')
    ->everyFifteenMinutes(); // 96 × 32 = 3072/день
```

#### Экономный режим (500-1000/день)
```php
// Вариант 1: Каждые 2 часа по 40
$schedule->command('recipes:parse --count=40')
    ->everyTwoHours(); // 12 × 40 = 480/день

// Вариант 2: Каждый час по 20
$schedule->command('recipes:parse --count=20')
    ->hourly(); // 24 × 20 = 480/день
```

#### Ночной режим
```php
// Парсинг только с 00:00 до 06:00
$schedule->command('recipes:parse --count=100')
    ->hourly()
    ->between('00:00', '06:00'); // 6 × 100 = 600/ночь
```

### Установка на web-хостинге

1. **Сделайте schedule.sh исполняемым:**
   ```bash
   chmod +x schedule.sh
   ```

2. **Откройте schedule.sh и укажите путь к проекту:**
   ```bash
   cd /home/username/domains/im-edok.ru/public_html || exit
   ```

3. **Добавьте в crontab хостинга:**
   ```
   */1 * * * * /home/username/domains/im-edok.ru/public_html/schedule.sh
   ```

4. **Или прямой вызов (без .sh файла):**
   ```
   */1 * * * * cd /home/username/domains/im-edok.ru/public_html && php artisan schedule:run >> /dev/null 2>&1
   ```

## 🔍 Как парсер находит нужное количество

### Алгоритм работы

```
1. Указываете: --count=32
2. Парсер идет на страницу 1
3. Получает ~50-60 URL с этой страницы
4. Проверяет в БД - допустим, 10 из них новые
5. Добавляет эти 10 в список (10/32)
6. Идет на страницу 2
7. Получает еще ~50-60 URL
8. Проверяет в БД - допустим, 15 новых
9. Добавляет в список (25/32)
10. Идет на страницу 3
11. Получает еще URL, находит 7 новых
12. Добавляет 7 в список (32/32) ✅
13. СТОП - цель достигнута!
14. Начинает парсить эти 32 URL по очереди
```

### Защита от зацикливания

- **Максимум 100 страниц** - если после 100 страниц не нашлось нужное количество, парсер остановится
- **5 пустых страниц подряд** - если подряд 5 страниц пустые, парсер остановится
- **Лимит попыток** - защита от бесконечных циклов

## ⏱️ Время выполнения

### Примерная длительность

| Количество | Время | Комментарий |
|------------|-------|-------------|
| 5 рецептов | ~30 сек | Быстрый тест |
| 10 рецептов | ~1 мин | Минимальная порция |
| 32 рецепта | ~2-3 мин | Стандарт для cron |
| 50 рецептов | ~4-5 мин | Средняя порция |
| 100 рецептов | ~8-10 мин | Большая порция |
| 500 рецептов | ~40-50 мин | Массовая загрузка |

**Примечание:** Время зависит от:
- Скорости интернета
- Загруженности источника
- Количества уже существующих рецептов в БД
- Случайных задержек (1-3 сек между рецептами)

## 🛡️ Защита и безопасность

### Автоматические задержки

```php
// Между рецептами - случайная задержка 1-3 секунды
sleep(rand(1, 3));

// Между страницами - 2 секунды
sleep(2);
```

### Проверка дубликатов

Парсер НЕ добавит рецепт если:
1. `source_url` уже есть в таблице `recipes`
2. В БД есть уникальный индекс на `source_url`

### User-Agent

Парсер использует реалистичный User-Agent браузера:
```
Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/120.0.0.0
```

## 📝 Структура логов

### Успешный парсинг
```
[2025-11-04 13:00:00] local.INFO: 🎯 Задача: найти 32 НОВЫХ рецептов
[2025-11-04 13:00:01] local.INFO: 🔍 Парсинг страницы 1
[2025-11-04 13:00:02] local.INFO: ✅ Страница 1: найдено 56 рецептов
[2025-11-04 13:00:02] local.INFO: 📊 Страница 1: из 56 рецептов, новых: 15
[2025-11-04 13:00:02] local.INFO: ✅ Собрано новых рецептов: 15/32
[2025-11-04 13:00:04] local.INFO: 🔍 Парсинг страницы 2
[2025-11-04 13:00:05] local.INFO: ✅ Страница 2: найдено 54 рецептов
[2025-11-04 13:00:05] local.INFO: 📊 Страница 2: из 54 рецептов, новых: 17
[2025-11-04 13:00:05] local.INFO: ✅ Собрано новых рецептов: 32/32
[2025-11-04 13:00:05] local.INFO: 🏁 Итого собрано НОВЫХ рецептов: 32/32
```

### Проблемы
```
[2025-11-04 13:00:10] local.WARNING: ⚠️ Страница 10: все рецепты уже в БД
[2025-11-04 13:00:12] local.WARNING: ⚠️ Страница 11 пустая (1/5)
[2025-11-04 13:00:15] local.ERROR: ❌ Ошибка парсинга страницы 15: Timeout
```

## 🎯 Достижение цели 1500+ рецептов/день

### Рекомендуемая настройка

```php
// В app/Console/Kernel.php
$schedule->command('recipes:parse --count=32')
    ->everyThirtyMinutes()
    ->appendOutputTo(storage_path('logs/parser.log'));
```

**Результат:**
- ✅ 48 запусков в день
- ✅ 32 рецепта за раз
- ✅ **1536 рецептов/день**

### Проверка достижения цели

```bash
# Запустите в конце дня
php artisan parser:stats

# Вы увидите:
# 📅 Сегодня: 1536
# ✅ Цель 1500+ рецептов/день достигнута!
```

## 🔧 Устранение неполадок

### Парсер находит меньше рецептов чем указано

**Причина:** Закончились новые рецепты на источнике  
**Решение:** Это нормально. Парсер вернет столько, сколько нашел

### Много дубликатов в логах

**Причина:** Большинство рецептов уже в БД  
**Решение:** Парсер правильно работает - фильтрует дубликаты

### Парсер долго выполняется

**Причина:** Задержки между запросами + поиск новых рецептов  
**Решение:** Это нормально, для безопасности

### Ошибки соединения

**Причина:** Проблемы с сетью или источник временно недоступен  
**Решение:** Парсер автоматически повторит при следующем запуске по cron

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `tail -f storage/logs/parser.log`
2. Запустите статистику: `php artisan parser:stats --detailed`
3. Протестируйте на 5 рецептах: `php artisan recipes:parse --count=5`
4. Проверьте расписание: `php artisan schedule:list`
