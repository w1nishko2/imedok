# 🔄 Гайд по бесконечному парсеру рецептов

## 📋 Описание

Новый парсер специально разработан для работы со страницей `https://1000.menu/cooking/all-new`, которая использует **бесконечный скролл** через пагинацию.

### Ключевые особенности:

- ✅ **Автоматическая пагинация** - парсер сам подгружает новые страницы через offset
- ✅ **Партийная обработка** - находит N рецептов, парсит их и сразу записывает в БД
- ✅ **Умная фильтрация** - проверяет существование URL в БД перед парсингом
- ✅ **Бесконечный режим** - может работать пока не закончатся рецепты
- ✅ **Продолжение с места остановки** - можно задать начальный offset

## 🚀 Использование

### Базовая команда

```bash
# Парсить ровно 50 новых рецептов
php artisan recipes:parse-infinite --max=50

# Парсить 100 рецептов партиями по 10
php artisan recipes:parse-infinite --max=100 --batch=10

# Бесконечный парсинг (пока есть рецепты)
php artisan recipes:parse-infinite

# Начать с определенного offset
php artisan recipes:parse-infinite --max=50 --offset=200
```

### Параметры команды

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--max` | Максимальное количество рецептов для добавления (0 = бесконечно) | 0 |
| `--batch` | Размер партии - сколько URL собрать перед парсингом | 5 |
| `--offset` | Начальный offset для пагинации (продолжить с места остановки) | 0 |

## 🎯 Как это работает

### Алгоритм работы:

1. **Сбор URL** с текущего offset (например, offset=0, limit=20)
2. **Фильтрация** - проверка, какие URL уже есть в БД
3. **Накопление** новых URL в партию (по умолчанию 5 штук)
4. **Парсинг партии** - как только накопилось 5 новых URL, парсим их
5. **Запись в БД** - сохраняем распарсенные рецепты
6. **Повтор** - переходим к следующему offset и продолжаем

### Пример работы:

```
Страница #1 (offset: 0): найдено 18, новых 15
  → Партия из 5 URL → Парсим → Сохраняем → Прогресс: 5 рецептов
  → Партия из 5 URL → Парсим → Сохраняем → Прогресс: 10 рецептов
  → Партия из 5 URL → Парсим → Сохраняем → Прогресс: 15 рецептов

Страница #2 (offset: 20): найдено 20, новых 12
  → Партия из 5 URL → Парсим → Сохраняем → Прогресс: 20 рецептов
  ...и так далее
```

## 📊 Статистика

После завершения парсинга вы получите подробную статистику:

```
╔════════════════════════════════════════════════════════╗
║   ✅ Парсинг завершен                                 ║
╚════════════════════════════════════════════════════════╝

┌─────────────────────┬──────────┐
│ Метрика             │ Значение │
├─────────────────────┼──────────┤
│ ⏱️  Время выполнения│ 245.6 сек│
│ 📄 Страниц обработано│ 5        │
│ 🔍 URL проверено    │ 98       │
│ 🆕 Найдено новых    │ 52       │
│ ✅ Добавлено в БД   │ 50       │
│ ❌ Ошибок парсинга  │ 2        │
│ 📊 Процент успеха   │ 96.15%   │
└─────────────────────┴──────────┘
```

## ⚙️ Настройка в cron

### Для регулярного пополнения базы:

```bash
# Каждый час добавлять 30 новых рецептов
0 * * * * cd /path/to/project && php artisan recipes:parse-infinite --max=30

# Каждые 30 минут добавлять 15 рецептов
*/30 * * * * cd /path/to/project && php artisan recipes:parse-infinite --max=15 --batch=5

# Раз в день ночью - большая партия
0 3 * * * cd /path/to/project && php artisan recipes:parse-infinite --max=200
```

## 🔍 Отладка

Для просмотра подробных логов:

```bash
# Логи парсера
tail -f storage/logs/laravel.log | grep -i "infinite"

# Только ошибки
tail -f storage/logs/laravel.log | grep "❌"

# Статистика
tail -f storage/logs/laravel.log | grep "📊\|✅\|🏁"
```

## 💡 Рекомендации

### Оптимальные настройки:

- **Для тестирования**: `--max=10 --batch=5`
- **Для регулярного пополнения**: `--max=30 --batch=5`
- **Для первого запуска**: `--max=500 --batch=10`
- **Для ночного парсинга**: `--max=1000 --batch=20`

### Безопасность:

- Между запросами страниц пауза 2-4 секунды (случайно)
- Между парсингом рецептов пауза 1-2 секунды
- Максимум 3 пустые страницы подряд - остановка

## 🛠️ Сервис в коде

Если нужно использовать в своем коде:

```php
use App\Services\InfiniteScrollParserService;
use App\Services\RecipeParserService;

$recipeParser = app(RecipeParserService::class);
$infiniteParser = new InfiniteScrollParserService($recipeParser);

// Настройка
$infiniteParser
    ->setBatchSize(10)
    ->setTargetUrl('https://1000.menu/cooking/all-new');

// Запуск
$stats = $infiniteParser->parseInfinitely(
    maxRecipes: 100,  // 0 = бесконечно
    startOffset: 0     // начальная позиция
);

// Результат
echo "Добавлено: {$stats['total_added']} рецептов";
```

## 🎓 Примеры использования

### 1. Быстрый тест (10 рецептов)
```bash
php artisan recipes:parse-infinite --max=10 --batch=5
```

### 2. Пополнить базу на 100 рецептов
```bash
php artisan recipes:parse-infinite --max=100 --batch=10
```

### 3. Продолжить с места остановки
```bash
# Если знаете последний offset (например, 240)
php artisan recipes:parse-infinite --max=50 --offset=240
```

### 4. Ночной автоматический сбор
```bash
# В crontab
0 2 * * * cd /var/www/im-edok && php artisan recipes:parse-infinite --max=500
```

## ❓ FAQ

**Q: Как узнать текущий offset?**  
A: Смотрите логи - там указывается offset каждой страницы.

**Q: Можно ли парсить другую страницу?**  
A: Да, используйте метод `setTargetUrl()` в коде сервиса.

**Q: Что делать если парсер зависает?**  
A: Проверьте логи, возможно проблемы с сетью. Парсер автоматически остановится после 3 пустых страниц.

**Q: Как ускорить парсинг?**  
A: Увеличьте `--batch` до 20-30, но учтите нагрузку на сайт.

## 📝 История изменений

- **v1.0** - Первая версия с поддержкой бесконечного скролла
- Поддержка пагинации через offset
- Партийная обработка
- Умная фильтрация существующих рецептов
