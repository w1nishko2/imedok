# Инструкция: Публикация подборок рецептов в Telegram

## Что было добавлено

### 1. Новый метод в TelegramService
- `publishRecipeCollection()` - публикует подборку из 5 случайных рецептов
- Поддерживает фильтрацию по категориям
- Красивое форматирование с эмодзи и ссылками

### 2. Новая команда Artisan
```bash
php artisan telegram:publish-collection
```

### Опции команды:
- `--category="Название"` - подборка из конкретной категории
- `--random-category` - случайная категория

## Примеры использования

### Смешанная подборка (топ-5 дня):
```bash
php artisan telegram:publish-collection
```

### Подборка из случайной категории:
```bash
php artisan telegram:publish-collection --random-category
```

### Подборка конкретной категории:
```bash
php artisan telegram:publish-collection --category="Десерты"
php artisan telegram:publish-collection --category="Супы"
php artisan telegram:publish-collection --category="Салаты"
```

## Настройка Crontab

### Вариант 1: Простая конфигурация
Подборки каждые 3 часа:
```cron
0 */3 * * * cd /home/g/gamechann2/im-edok_ru && php8.1 artisan telegram:publish-collection --random-category >> storage/logs/telegram-collection-cron.log 2>&1
```

### Вариант 2: Полная конфигурация (РЕКОМЕНДУЕТСЯ)
```cron
# Отдельные рецепты каждые 10 минут
*/10 * * * * cd /home/g/gamechann2/im-edok_ru && php8.1 artisan telegram:publish-recipe >> storage/logs/telegram-cron.log 2>&1

# Подборки каждые 3 часа
0 */3 * * * cd /home/g/gamechann2/im-edok_ru && php8.1 artisan telegram:publish-collection --random-category >> storage/logs/telegram-collection-cron.log 2>&1
```

### Вариант 3: Тематические подборки в определенное время
```cron
# Утренняя подборка завтраков (8:00)
0 8 * * * cd /home/g/gamechann2/im-edok_ru && php8.1 artisan telegram:publish-collection --category="Завтраки" >> storage/logs/telegram-collection-cron.log 2>&1

# Дневная подборка (13:00)
0 13 * * * cd /home/g/gamechann2/im-edok_ru && php8.1 artisan telegram:publish-collection --random-category >> storage/logs/telegram-collection-cron.log 2>&1

# Вечерняя подборка (19:00)
0 19 * * * cd /home/g/gamechann2/im-edok_ru && php8.1 artisan telegram:publish-collection --random-category >> storage/logs/telegram-collection-cron.log 2>&1
```

## Формат публикации

Подборка выглядит так:

```
✨ Топ-5 рецептов дня

Мы подобрали для вас 5 отличных рецептов:

1. 🍰 Шоколадный торт
   ⏰ 60 мин · 🔥 450 ккал

2. 🥗 Греческий салат
   ⏰ 15 мин · 🔥 180 ккал

3. 🍲 Борщ украинский
   ⏰ 90 мин · 🔥 320 ккал

4. 🍝 Паста Карбонара
   ⏰ 25 мин · 🔥 550 ккал

5. 🥧 Яблочный пирог
   ⏰ 50 мин · 🔥 280 ккал

━━━━━━━━━━━━━━━━━━
👨‍🍳 Готовьте с удовольствием!
📢 Наш канал: https://t.me/imedokru
🌐 Сайт: https://im-edok.ru

#подборка #рецепты #яедок
```

## Преимущества подборок

1. ✅ **Разнообразие контента** - не только отдельные рецепты, но и подборки
2. ✅ **Совместимость с Дзеном** - публикуются БЕЗ кнопок
3. ✅ **Удобная навигация** - все рецепты со ссылками
4. ✅ **Тематическое распределение** - можно публиковать по категориям
5. ✅ **Автоматизация** - запуск по расписанию через cron

## Логи и мониторинг

### Просмотр логов подборок:
```bash
tail -f storage/logs/telegram-collection-cron.log
```

### Просмотр логов отдельных рецептов:
```bash
tail -f storage/logs/telegram-cron.log
```

### Просмотр общих логов Laravel:
```bash
tail -f storage/logs/laravel.log
```

## Требования

- Минимум 5 рецептов в базе данных
- Для подборок по категориям - минимум 5 рецептов в категории
- Настроенный Telegram Bot с доступом к каналу

## Статистика публикаций

### При рекомендуемой настройке:
- **Отдельные рецепты:** 144 поста в день (каждые 10 минут)
- **Подборки:** 8 подборок в день (каждые 3 часа) = 40 рецептов
- **ИТОГО:** 152 поста в день

### Альтернативные варианты:
- **Каждые 2 часа:** 12 подборок в день = 60 рецептов
- **Каждые 4 часа:** 6 подборок в день = 30 рецептов
- **Каждые 6 часов:** 4 подборки в день = 20 рецептов

## Тестирование

### Тест публикации смешанной подборки:
```bash
php artisan telegram:publish-collection
```

### Тест со случайной категорией:
```bash
php artisan telegram:publish-collection --random-category
```

### Тест конкретной категории:
```bash
php artisan telegram:publish-collection --category="Десерты"
```

## Устранение проблем

### Ошибка "Not enough recipes":
- Убедитесь, что в базе есть минимум 5 рецептов
- Для категорийных подборок - минимум 5 рецептов в категории

### Подборка не публикуется:
1. Проверьте соединение с ботом: `php artisan telegram:publish-recipe --test`
2. Проверьте логи: `tail -f storage/logs/laravel.log`
3. Убедитесь, что бот имеет права администратора в канале

### Проблемы с cron:
1. Проверьте список задач: `crontab -l`
2. Проверьте логи cron: `tail -f storage/logs/telegram-collection-cron.log`
3. Запустите команду вручную для тестирования

## Заключение

Теперь у вас есть два типа контента для Telegram канала:
1. **Отдельные рецепты** - регулярные публикации каждые 10 минут
2. **Подборки из 5 рецептов** - каждые 3 часа для разнообразия

Оба типа публикаций совместимы с Яндекс.Дзеном и будут автоматически синхронизироваться с вашим каналом в Дзене!
